// VARIABLE DECLARATION QUES 7 NOTES 
#include <stdio.h>

int main() {
    // Declaration without initialization
    int number;

    // Declaration with initialization
    int sum = 0;

    // Using the variables
    number = 42;
    sum = sum + number;

    printf("The sum is: %d\n", sum);

    return 0;
}
